#include <stdio.h>
#include <stdlib.h>

int main()
{
	int tab[4], i = 0;
	tab[0] = 1;
	tab[1] = 5;
	tab[2] = 8;
	tab[3] = 3;
    
    for (i = 0 ; i <4 ; i++)
    {
	     printf("%d\n", tab[i]);
    }
	return (0);
}
